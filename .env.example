# The dashboard requires no credentials — it reads a local DuckDB file.

# Build-time ingestion only (make ingest) — never needed to run the dashboard.
export KAGGLE_API_TOKEN=your-kaggle-api-token
export SETLISTFM_API_KEY=your-setlistfm-api-key
